{
  "name": "timezoner-server",
  "version": "1.0.0",
  "description": "simple node/express server to make API calls for timezoner-extension",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "deploy": "now remove timezoner-server --safe -y && now --public && now alias",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JavaScript Joe",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "dotenv": "^6.0.0",
    "nodemon": "^1.18.3"
  }
}
